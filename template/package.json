{
  "name": "next-elba-template",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 4000",
    "dev:inngest": "inngest-cli dev -u http://localhost:4000/api/inngest",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "database:up": "docker-compose -p integration --profile edge --env-file=.env.local up -d",
    "database:down": "docker-compose -p integration down --volumes",
    "database:migrate": "drizzle-kit push:pg --config ../../drizzle.config.ts",
    "database:generate": "drizzle-kit generate:pg --config ../../drizzle.config.ts"
  },
  "dependencies": {
    "@elba-security/config": "workspace:*",
    "@elba-security/inngest": "workspace:*",
    "@elba-security/logger": "workspace:*",
    "@elba-security/nextjs": "workspace:*",
    "@elba-security/sdk": "workspace:*",
    "@elba-security/utils": "workspace:*",
    "@neondatabase/serverless": "0.9.4",
    "@sentry/nextjs": "^7.88.0",
    "drizzle-orm": "^0.29.4",
    "inngest": "3.19.11",
    "next": "14.2.4",
    "react": "^18",
    "react-dom": "^18",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^3.2.0",
    "@elba-security/eslint-config-custom": "workspace:*",
    "@elba-security/test-utils": "workspace:*",
    "@elba-security/tsconfig": "workspace:*",
    "@next/eslint-plugin-next": "^14.2.4",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.20.6",
    "eslint": "^8",
    "inngest-cli": "^0.29.1",
    "msw": "^2.3.1",
    "postgres": "^3.4.4",
    "testcontainers": "^10.10.0",
    "typescript": "^5.5.2",
    "vitest": "1.6.0"
  }
}
